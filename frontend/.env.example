# Supabase Configuration
PUBLIC_SUPABASE_URL=your_supabase_url
PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_KEY=your_supabase_service_key
SUPABASE_PASSWORD=your_password
# Bot Supabase Configuration (if using separate bot database)
PUBLIC_BOT_SUPABASE_URL=your_bot_supabase_url
PUBLIC_BOT_SUPABASE_ANON_KEY=your_bot_supabase_anon_key

# Flow Network Configuration
PUBLIC_FLOW_NETWORK=testnet

# AI Agent Configuration (Server-side only - NEVER use PUBLIC_ prefix)
# Choose one of the following LLM providers:

# Option 1: OpenAI
OPENAI_API_KEY=your_openai_api_key

# Option 2: Anthropic Claude
ANTHROPIC_API_KEY=your_anthropic_api_key

# SECURITY WARNING:
# - NEVER commit actual API keys to version control
# - NEVER use PUBLIC_ prefix for API keys (except for public Supabase anon keys)
# - API keys should only be in server-side environment variables
# - Add .env to .gitignore
